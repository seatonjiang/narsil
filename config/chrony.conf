# 指定记录时钟漂移值的文件路径，用于长期修正系统时钟
driftfile /var/lib/chrony/chrony.drift

# 指定日志文件的存放目录
logdir /var/log/chrony

# 当系统时钟偏移超过 100 ppm 时，限制更新速率，防止时钟跳变
maxupdateskew 100.0

# 启用 RTC（实时时钟）同步，使硬件时钟与系统时钟保持一致
rtcsync

# 在启动时或时钟偏移超过 1 秒时，允许在前 3 次更新中立即步进调整时间
makestep 1 3

# 时间服务器列表
